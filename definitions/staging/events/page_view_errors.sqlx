config {
  type: "incremental",
  uniqueKey:['dt','event_id'],
  schema: "dataform_staging",
  bigquery: {
    partitionBy: "dt",
  }
}

select
    * except (is_valid)
from ${ref("page_view_tmp")} p
where is_valid is not true